extends ../../layout/layoutIndex

block title
  title Events Slider

block main
  .row.center-xs
    .col-xs-8
      h3 Sources Information
  .row.center-xs
    .col-xs-10
      h5 Titulo: #{source.name}
      h5 Imagen: #{source.image}
      h5 Foto
      img(src="/images/imagesSources/" + source.image)
